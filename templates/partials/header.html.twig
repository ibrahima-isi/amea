<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-graduation-cap"></i> <strong class="text-light-beige">AEESGS</strong>
            </a>
            <div class="d-flex align-items-center order-lg-3">
                <a href="#" class="nav-link theme-switcher px-2" title="Changer le thème">
                    <i class="fas fa-moon fs-5 d-lg-none"></i>
                    <span class="theme-label d-none d-lg-inline">Mode sombre</span>
                </a>
                
                {% if session.user_id %}
                    <a href="logout.php" class="nav-link px-2 text-danger" title="Déconnexion">
                        <i class="fas fa-sign-out-alt fs-5 d-lg-none"></i>
                        <span class="d-none d-lg-inline">Déconnexion</span>
                    </a>
                {% else %}
                    <a href="login.php" class="nav-link px-2 {{ login_active ? 'active' : '' }}" title="Connexion">
                        <i class="fas fa-sign-in-alt fs-5 d-lg-none"></i>
                        <span class="d-none d-lg-inline">Connexion</span>
                    </a>
                {% endif %}

                <button class="navbar-toggler ms-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>

            <div class="collapse navbar-collapse order-lg-2" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item">
                        <a class="nav-link {{ index_active ? 'active' : '' }}" href="/">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ register_active ? 'active' : '' }}" href="register.php">S'enregistrer</a>
                    </li>
                    {% if session.user_id %}
                        <li class="nav-item">
                            <a class="nav-link" href="dashboard.php">Tableau de bord</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
</header>
